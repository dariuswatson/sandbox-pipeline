apiVersion: v1
kind: Service
metadata:
  name: {{ include "sandbox-pipeline.fullname" . }}
  labels:
    {{- include "sandbox-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sandbox-pipeline.selectorLabels" . | nindent 4 }}
